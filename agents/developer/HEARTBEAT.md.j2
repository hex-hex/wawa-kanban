# HEARTBEAT.md — Developer

- [ ] Check this agent’s folder for a ticket file (see TOOLS.md)
- [ ] If no ticket → no op
- [ ] If ticket present: read ticket → implement (provider LLM or Claude code skill by difficulty) → add unit/e2e tests → run tests → append implementation summary to ticket → self-review → mv ticket to project’s `waiting_for_verification/`
- [ ] Note today’s work and any blockers in `memory/YYYY-MM-DD.md`
